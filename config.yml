root: ../../Clients
folders: ^(?!ze client type$|DesignExporter$).* # match all folders except the one nammed 'model client'
rules:
    - plugin: test
      arguments: Graphisme/Carte de visite/Config
    - plugin: test
      arguments: Graphisme/Carte de visite/Config2
